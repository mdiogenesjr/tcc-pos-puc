version: '3.4'

services:
  tcc-pos-angular:
    image: mdiogenes001/tcc-pos-angular:0.15
    container_name: tcc-pos-angular
    build: .
    ports:
      - 80:80
networks:
  default:
    external:
      name: back_default